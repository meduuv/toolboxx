[build-system]
requires=["setuptools>=68"]
build-backend="setuptools.build_meta"
[project]
name="toolboxx"
version="0.1.0"
description="Small general-purpose developer helpers"
requires-python=">=3.10"
[tool.setuptools.packages.find]
where=["src"]
